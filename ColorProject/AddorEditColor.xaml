<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:colorproject="clr-namespace:ColorProject"
             x:DataType="colorproject:AddorEditViewModel"
             x:Class="ColorProject.AddorEditColor">
    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="Colour Name" Keyboard="Text" Margin="0,20,0,20" TextColor="Black" FontSize="Large" Text="{Binding Coloured.ColorName}"/>
            <Entry Placeholder="Colour"  Text="{Binding Coloured.Coloring}" />
            <!--<Entry Placeholder="Colour" Keyboard="Numeric" Text="{Binding R}" />
            <Entry Placeholder="Colour" Keyboard="Numeric" Text="{Binding G}" />
            <Entry Placeholder="Colour" Keyboard="Numeric" Text="{Binding B}" />-->


            <Grid  Margin="0,280,0,0">
                <Button Text="Save" BackgroundColor="Green" TextColor="white" Margin="80,0,80,0" HorizontalOptions="Start"  WidthRequest="80" CornerRadius="30" Clicked="Save"></Button>
                <Button Text="Discard" BackgroundColor="Red" TextColor="White"  HorizontalOptions="End" Margin="80,0,80,0"  WidthRequest="80"   CornerRadius="30" Clicked="Discard"/>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>